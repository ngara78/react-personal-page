import React from 'react';

const SectionHeader = () => {
    return (
        <header className="sections__header">
            <h1 className="title">Well.. Hi there!</h1>
        </header>
    );
};

export default SectionHeader;